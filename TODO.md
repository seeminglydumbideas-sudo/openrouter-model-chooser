# some todos or idea to explore

* opencode config support other options we may tune per model
  * model / small_model : cheap model for small task (naming titles, etc.)
 * agent primaire : voir si on peu en configurer à côté de plan and build. si c'est utile ou meme nécessaire. quel use-case?
 * agents secondaires : plus intéressant car peuvent etre pour des taches spécialisé (coder, plannifier, troubleshoot, reflexion, critical review) peut etre faire des role comme une dream team.
 * there are built-in sub-agents too : explore, scout, compaction, title, summary.
   * what are their description? prompt? models?
   * can we defined which model for each?
   * is there place for our own agents aside of them?
   * add agents with description and prompt
     * coding -> coding model
     * ? -> agent model
     * ? -> intelligent model
     * ? -> costly but more performant model
     * and set default_agent
 

model
small_model
default_agent 



jarvis (primary) -> just5 verify o
-> sub-agents